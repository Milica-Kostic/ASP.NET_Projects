To enable Put/Delete on your web server (if it isn't working), add this to the web.config file that is in
the root of your publish directory


<system.WebServer>
	<modules runAllManagedModulesForAllRequests="false">
		<remove name="WebDAVModule"
	</modules>
</system.WebServer>