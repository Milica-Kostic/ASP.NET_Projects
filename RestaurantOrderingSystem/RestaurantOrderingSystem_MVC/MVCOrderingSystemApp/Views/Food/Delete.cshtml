@model RestaurantDataLibrary.Models.FoodModel
@{
    ViewData["Title"] = "Delete";
}

@if (Model is null)
{
    <h3>The requested record was not found.</h3>
}
else
{
    <div class="h4">
        Are you sure you want to delete @Model.FoodName?
    </div>
    <form asp-action="Delete">
        <input asp-for="Id" value="@Model.Id" type="hidden" />
        <div class="form-group">
            <input type="submit" value="Delete" class="btn btn-danger" />
            <a href="/Food/Display/@Model.Id" class="btn btn-outline-danger">Cancel</a>
        </div>
    </form>
}